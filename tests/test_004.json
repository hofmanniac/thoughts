[
  { "#when": "test4", "#then": "assert4" },

  {
    "#when": "assert4",
    "#then": "val4"
  },

  {
    "#when": "run-test",
    "#then": [
      { "#assert": "test4", "into": "?result" },
      { "#if": "?result", "equals": "val4", "#then": "pass", "#else": "fail" }
    ]
  }
]
