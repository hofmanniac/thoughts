[
  { "#when": "test1", "#then": "val1" },

  {
    "#when": "run-test",
    "#then": [
      { "#assert": "test1", "into": "?result" },
      { "#if": "?result", "equals": "val1", "#then": "pass", "#else": "fail" }
    ]
  }
]
